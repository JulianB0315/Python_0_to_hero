# Modulos

>[!IMPORTANT]
>## Recordatorio:
>1. Los ejercicios se separan por módulos como “Hola mundo” conteniendo de 2 a 4 actividades relacionadas con la clase previamente leída. 
>2. Para mandar tu respuesta recuerda hacer un pull requests el la carpeta del módulo y también mandar el archivo con el formato **"[titulo del modulo] - [Nombre de usuario de GitHub]"**

>[!NOTE]
> ### Actividades
> ### **Ejercicio 1**:  
> Escribe una función llamada `generar_id_usuario`. La función generará un ID aleatorio de seis dígitos o caracteres.  
> ```python
> # Salida esperada (un ejemplo):
> # generar_id_usuario()
> # '1ee33d'
> ```
> ### **Ejercicio 2**:  
> Modifica el ejercicio anterior. Declara una función llamada `generar_ids_por_usuario`. La función no recibe parámetros directamente, pero pide al usuario dos entradas:  
> 1. El número de caracteres para cada ID.  
> 2. El número de IDs que se deben generar.  
> ```python
> # Ejemplo de entrada del usuario: 5 5
> # Salida esperada:
> # kcsy2
> # SMFYb
> # bWmeq
> # ZXOYh
> # 2Rgxf
> 
> # Otro ejemplo de entrada: 16 5
> # Salida esperada:
> # 1GCSgPLMaBAVQZ26
> # YD7eFwNQKNs7qXaT
> # ycArC5yrRupyG00S
> # UbGxOFI7UXSWAyKN
> # dIV0SSUTgAdKwStr
> ```
> ### **Ejercicio 3**:  
> Escribe una función llamada `generar_color_rgb`. Esta función generará colores RGB aleatorios, donde cada valor del color (rojo, verde, azul) estará en el rango de 0 a 255.  
> ```python
> # Salida esperada:
> # generar_color_rgb()
> # 'rgb(125,244,255)'
> ```
> ### **Ejercicio 4**:  
> Escribe una función llamada `lista_colores_hexadecimales` que devuelva una lista de colores hexadecimales. La cantidad de colores será un parámetro que reciba la función.
> ```python
> # Ejemplo:
> # lista_colores_hexadecimales(3)
> # ['#a3e12f', '#03ed55', '#eb3d2b']
> ```
> ### **Ejercicio 5**:  
> Escribe una función llamada `lista_colores_rgb` que devuelva una lista de colores RGB. La cantidad de colores será un parámetro que reciba la función.
> ```python
> # Ejemplo:
> # lista_colores_rgb(3)
> # ['rgb(5, 55, 175)', 'rgb(50, 105, 100)', 'rgb(15, 26, 80)']
> ```
> ### **Ejercicio 6**:  
> Escribe una función llamada `generar_colores` que pueda generar una lista de colores hexadecimales o RGB. La función recibirá dos parámetros:  
> 1. El tipo de color: `'hexa'` o `'rgb'`.  
> 2. La cantidad de colores que se quieren generar.  
> ```python
> # Ejemplo:
> # generar_colores('hexa', 3)
> # ['#a3e12f', '#03ed55', '#eb3d2b']
> 
> # generar_colores('rgb', 2)
> # ['rgb(33,79,176)', 'rgb(120,55,90)']
> ```


>[!TIP]
>## Y recuerda: 
># "Si puedes imaginarlo puedes programarlo"